'use strict';

const _ = require('lodash');
const assert = require('chai').assert;
const propertiesFormatUriRule = require('../../../lib/rules/properties-format-uri');

describe('properties-format-uri', () => {
  const options = true;
});
